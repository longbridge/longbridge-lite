[package]
name = "longbridge-lite"
version = "0.5.2"
edition = "2024"
publish = false

[dependencies]
gpui-omarchy-shell = { git = "https://github.com/huacnlee/gpui-omarchy", rev = "671cab6bb770387d41dea1c968f52bc06d6d9d16", features = ["gpui-shell"] }
gpui-shell = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67" }
gpui-kit = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67", default-features = false }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
rustls = { version = "0.23", default-features = false, features = ["ring"] }
gpui-kit = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67", default-features = false, features = ["test-support"] }

[profile.dev]
codegen-units = 16
debug = "limited"
split-debuginfo = "unpacked"

# Every dependency, not a list of the ones that looked hot.
#
# A frame is spent almost entirely inside crates this application does not
# write: GPUI paints it, `taffy` lays it out, `zed-font-kit` shapes the text,
# `resvg` rasterizes the icons, `etagere` packs the atlas. Naming a few of them
# left the rest at `opt-level = 0`, and the ones left out are on the same
# frame: a debug build drew in 40-45 ms against release's 6-15 ms, and moved to
# 19-23 ms once the list stopped being a list.
#
# The cost is a slower first build. It is paid once per dependency version,
# while the frame is paid on every interaction, all day.
[profile.dev.package."*"]
opt-level = 3

[build-dependencies]
gpui-omarchy-shell = { git = "https://github.com/huacnlee/gpui-omarchy", rev = "671cab6bb770387d41dea1c968f52bc06d6d9d16" }

[patch.crates-io]
gpui-kit = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67" }
gpui-base = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67" }
gpui-kit-assets = { git = "https://github.com/longbridge/gpui-kit", rev = "4b383ec8a47e14d4e19759535442e783df99ff67" }
